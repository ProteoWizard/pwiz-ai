[project]
name = "teamcity-mcp"
version = "0.1.0"
description = "MCP server for TeamCity CI build monitoring"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
teamcity-mcp = "server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
